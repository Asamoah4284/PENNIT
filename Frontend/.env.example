# Backend API base URL (no trailing slash).
# Leave empty to use same origin (dev proxy to localhost:3000).
# For production or remote backend, set to your API URL, e.g.:
# VITE_API_URL=https://your-app.onrender.com
VITE_API_URL=
