# Backend API base URL (no trailing slash).
# Leave empty to use same origin (dev proxy to localhost:3000).
#
# Production (e.g. Vercel): set this in your hosting env vars to your backend URL.
# Example: VITE_API_URL=https://pennit-api.onrender.com
# Without this, the app will request /api/* from the frontend host and get 404.
VITE_API_URL=api.pennit.io
